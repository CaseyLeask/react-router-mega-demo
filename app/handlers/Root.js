/** @jsx React.DOM */
var React = require('react');
var Promise = require('when');

var Root = module.exports = React.createClass({

  render: function() {
    return (
      <h1>{'Yo!'}</h1>
    );
  }

});
